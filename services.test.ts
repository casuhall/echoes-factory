import { Entité } from "./domain";
import { } from "./repositories";
import { Usine } from "./services";


describe("Vérification du fonctionnement de l'usine", () => {
  let usine: Usine;
  beforeEach(() => {
    usine = new Usine("Test");
  });

  it.todo(`Cas nominal : Initialisation d'une usine vide`);

  it.todo(`Cas nominal : Initialisation d'une usine vide`);

  it.todo(`Cas d'erreur : étude de marché avec deux recettes qui font référence l'une à l'autre`);

});
